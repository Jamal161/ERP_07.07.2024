
@{
    ViewBag.Title = "TodayTask";
}

@RenderPage("~/Views/Shared/Sidebar.cshtml")


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Exam List</title>

    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto|Varela+Round">
    <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.1/moment.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment-timezone/0.5.36/moment-timezone-with-data.min.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>





    <style>
        body {
            font-family: 'Varela Round', sans-serif;
            font-size: 13px;
            max-width: 100%;
        }

        .table-responsive {
            margin: 100px 0;
        }

        .table-wrapper {
            min-width: 1550px;
            background: #fff;
            padding: 20px 25px;
            margin-left: -40px;
            border-radius: 25px;
            box-shadow: 0 1px 1px rgba(0,0,0,.05);
            margin-top: 80px;
        }

        .table-title {
            padding-bottom: 15px;
            padding: 16px 30px;
            margin: -20px -25px 10px;
            border-radius: 3px 3px 0 0;
        }

            .table-title h2 {
                margin: 5px 0 0;
                font-size: 24px;
            }

            .table-title .btn-group {
                float: right;
            }

            .table-title .btn {
                color: #fff;
                float: right;
                font-size: 13px;
                border: none;
                min-width: 50px;
                border-radius: 2px;
                border: none;
                outline: none !important;
                margin-left: 10px;
            }

                .table-title .btn i {
                    float: left;
                    font-size: 21px;
                    margin-right: 5px;
                }

                .table-title .btn span {
                    float: left;
                    margin-top: 2px;
                }

        table.table tr th,
        table.table tr td {
            border-color: #e9e9e9;
            padding: 12px 15px;
            vertical-align: middle;
        }

            table.table tr th:first-child {
                width: 60px;
            }

            table.table tr th:last-child {
                width: 100px;
            }

        table.table-striped tbody tr:nth-of-type(odd) {
            background-color: #fcfcfc;
        }

        table.table-striped.table-hover tbody tr:hover {
            background: #f5f5f5;
        }

        table.table th i {
            font-size: 13px;
            margin: 0 5px;
            cursor: pointer;
        }

        table.table td:last-child i {
            opacity: 0.9;
            font-size: 22px;
            margin: 0 5px;
        }

        table.table td a {
            font-weight: bold;
            color: #566787;
            display: inline-block;
            text-decoration: none;
            outline: none !important;
        }

            table.table td a:hover {
                color: #2196F3;
            }

            table.table td a.edit {
                color: #FFC107;
            }

            table.table td a.delete {
                color: #F44336;
            }

        table.table td i {
            font-size: 19px;
        }

        table.table .avatar {
            border-radius: 50%;
            vertical-align: middle;
            margin-right: 10px;
            display: inline-block;
        }

        .pagination {
            float: right;
            margin: 0 0 5px;
        }

            .pagination li a {
                border: none;
                font-size: 13px;
                min-width: 30px;
                min-height: 30px;
                color: #999;
                margin: 0 2px;
                line-height: 30px;
                border-radius: 2px !important;
                text-align: center;
                padding: 0 6px;
            }

                .pagination li a:hover {
                    color: #666;
                }

            .pagination li.active a,
            .pagination li.active a.page-link {
                background: #03A9F4;
            }

                .pagination li.active a:hover {
                    background: #0397d6;
                }

            .pagination li.disabled i {
                color: #ccc;
            }

            .pagination li i {
                font-size: 16px;
                padding-top: 6px;
            }

        .hint-text {
            float: left;
            margin-top: 10px;
            font-size: 13px;
        }

        .nav-tabs .nav-item .nav-link.active {
            background-color: #007bff;
            color: #fff;
        }

        .nav-tabs .nav-item .nav-link {
            background-color: #f8f9fa;
            color: #212529;
            border-color: transparent;
        }


        .nav-tabs .nav-item {
            margin-right: 10px;
        }

            .nav-tabs .nav-item .nav-link {
                text-decoration: none;
            }


        .btn-success {
            color: white;
            background-color: green;
            border: none;
            padding: 8px 8px;
            border-radius: 4px;
            cursor: pointer;
            margin-left: 65px;
        }

            .btn-success:hover {
                background-color: darkgreen;
            }

            .btn-success i {
                vertical-align: middle;
                margin-right: 5px;
            }
    </style>
</head>
<body>
    @*<div class="container">
        <div class="table-responsive">
            <div class="table-wrapper">
                <ul class="nav nav-tabs" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" data-toggle="tab" href="#tabs-1" role="tab">Upcoming</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" data-toggle="tab" href="#tabs-2" role="tab">Past</a>
                    </li>
                    <li class="nav-item" style="float: right;">
                        <button class="btn btn-success" onclick="location.href = '/Home/DailyTaskAdd';">
                            <i class="material-icons">&#xE147;</i> Add DailyTask
                        </button>

                    </li>
                </ul>

                <br />
                <div class="tab-content">
                    <div class="tab-pane active" id="tabs-1" role="tabpanel">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Exam Name</th>
                                    <th>Valid From</th>
                                    <th>Valid To</th>
                                    <th>Marks</th>
                                    <th>Duration</th>
                                    <th class="h-0 d-inline-block" style="width: 150px;">Result Overview</th>
                                </tr>
                            </thead>
                            <tbody class="tbody-upcoming"></tbody>
                        </table>
                        <br>
                        <div class="clearfix" style="float:right;">
                            <a href="#" class="btn btn-primary btnPreviousUpcoming">Previous</a>
                            <a href="#" class="btn btn-primary btnNextUpcoming">Next</a>
                        </div>
                        <br />
                        <br />
                        <br />

                    </div>
                    <div class="tab-pane" id="tabs-2" role="tabpanel">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>Exam Name</th>
                                    <th>Valid From</th>
                                    <th>Valid To</th>
                                    <th>Marks</th>
                                    <th>Duration</th>
                                    <th class="h-0 d-inline-block" style="width: 150px;">Result Overview</th>
                                </tr>
                            </thead>
                            <tbody class="tbody-past"></tbody>
                        </table>
                        <br>
                        <div class="clearfix" style="float:right;">
                            <a href="#" class="btn btn-primary btnPreviousPast">Previous</a>
                            <a href="#" class="btn btn-primary btnNextPast">Next</a>
                        </div>
                    </div>
                </div>

                <br />
                <br />
                <br />


            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>




        $(document).ready(function () {
            loadData('upcoming');
            loadData('past');
        });

        var pageSize = 25;
        var currentPageUpcoming = 1;
        var currentPagePast = 1;

        function loadData(tab) {
            var currentPage = tab === 'upcoming' ? currentPageUpcoming : currentPagePast;
            var startIndex = (currentPage - 1) * pageSize;
            var endIndex = startIndex + pageSize;

            var apiUrl = '/Exam/mGetExamList';

            $.ajax({
                url: apiUrl,
                type: 'GET',
                success: function (response) {
                    var data = tab === 'upcoming' ? response.UpcomingExams : response.PastExams;


                    var uniqueData = [];
                    var examIds = {};
                    $.each(data, function (index, item) {
                        if (!examIds[item.strexamId]) {
                            uniqueData.push(item);
                            examIds[item.strexamId] = true;
                        }
                    });

                    var filteredData = uniqueData.slice(startIndex, endIndex);

                    var html = '';
                    $.each(filteredData, function (index, item) {
                        html += '<tr>';
                        html += '<td><div class="h-0 d-inline-block" style="width: 250px;">' + item.strExamTitle + '</div></td>';
                        html += '<td>' + item.strexamDate + ' ' + item.strstarttime + '</td>';
                        html += '<td>' + item.strexamDate + ' ' + item.strendtime + '</td>';
                        html += '<td>' + item.strtotalMarks + '</td>';
                        html += '<td>' + item.strtimeLimit + ' min</td>';
                        html += '<td><button class="btn btn-success" onclick="viewExamResult(\'' + item.strexamId + '\')"><span class="fa-stack fa-lg pull-left"><i class="fa fa-eye fa-stack-1x "></i></span></button></td>';
                        //html += '<td><button class="btn btn-success" onclick="location.href=\'/Home/ExamResult\'"><span class="fa-stack fa-lg pull-left"><i class="fa fa-eye fa-stack-1x "></i></span></button></td>';
                        html += '</tr>';


                    });

                    if (tab === 'upcoming') {
                        $('.tbody-upcoming').html(html);
                        updatePaginationControls(currentPage, uniqueData.length, tab);
                    } else if (tab === 'past') {
                        $('.tbody-past').html(html);
                        updatePaginationControls(currentPage, uniqueData.length, tab);
                    }
                },
                error: function (xhr, status, error) {
                    console.log('Error:', error);
                }
            });
        }

        function updatePaginationControls(currentPage, totalItems, tab) {
            var totalPages = Math.ceil(totalItems / pageSize);
            if (tab === 'upcoming') {
                $('.btnPreviousUpcoming').prop('disabled', currentPage === 1);
                $('.btnNextUpcoming').prop('disabled', currentPage === totalPages);
            } else if (tab === 'past') {
                $('.btnPreviousPast').prop('disabled', currentPage === 1);
                $('.btnNextPast').prop('disabled', currentPage === totalPages);
            }
        }

        function viewExamResult(examId) {
            window.location.href = '/Home/ExamResult?examId=' + examId;
        }

        $('.btnPreviousUpcoming').click(function () {
            if (currentPageUpcoming > 1) {
                currentPageUpcoming--;
                loadData('upcoming');
            }
        });

        $('.btnNextUpcoming').click(function () {
            currentPageUpcoming++;
            loadData('upcoming');
        });

        $('.btnPreviousPast').click(function () {
            if (currentPagePast > 1) {
                currentPagePast--;
                loadData('past');
            }
        });

        $('.btnNextPast').click(function () {
            currentPagePast++;
            loadData('past');
        });


    </script>*@




    <div class="container">
        <div class="table-responsive">
            <div class="table-wrapper">
                <ul class="nav nav-tabs" role="tablist">
                    <li class="nav-item">
                        <a class="nav-link active" data-toggle="tab" href="#tabs-1" role="tab">Daily Task List</a>
                    </li>
                    <li class="nav-item" style="float: right;">
                        <button class="btn btn-success" onclick="location.href = '/Dashboard/DailyTaskAdd';">
                            <i class="material-icons">&#xE147;</i> Add DailyTask
                        </button>
                    </li>
                </ul>

                <br />
                <div class="tab-content">
                    <div class="tab-pane active" id="tabs-1" role="tabpanel">
                        <table class="table table-striped table-hover">
                            <thead>
                                <tr>
                                    <th>ID</th>
                                    <th>Title</th>
                                    <th>Valid From</th>
                                    <th>Valid To</th>
                                    <th>Announcement</th>
                                    <th>Active</th>
                                  
                                </tr>
                            </thead>
                            <tbody class="tbody"></tbody>
                        </table>
                        <br>
                        <div class="clearfix" style="float:right">
                            <a href="#" class="btn btn-primary btnPrevious" id="paginationPrevious">&laquo; Previous</a>
                            <a href="#" class="btn btn-primary btnNext" id="paginationNext">Next &raquo;</a>
                        </div>
                        <br />
                        <br />
                        <br />
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        $(document).ready(function () {
            loadData('all');
        });

        var currentPage = 1;
        var currentStatus = 'all';
        var pageSize = 15;

        function loadData(status, page = 1) {
            var startIndex = (page - 1) * pageSize;
            var endIndex = startIndex + pageSize;

            $.ajax({
                url: "/DailyTask/mGetdailyTasktodayList",
                type: "GET",
                contentType: 'application/json;charset=utf-8',
                dataType: "json",
                data: JSON.stringify({ status: status }),
                success: function (result) {

                    console.log(result)
                   
                    result.sort(function (a, b) {
                        return new Date(b.strDeadline) - new Date(a.strDeadline);
                    });

                    var slicedData = result.slice(startIndex, endIndex);
                    var html = '';
                    $.each(slicedData, function (key, item) {
                        html += '<tr>';
                        html += '<td>' + item.strTask_ID + '</td>';
                        html += '<td><div class="h-0 d-inline-block" style="width: 150px;">' + item.strTitle + '</div></td>';
                        html += '<td><div class="h-0 d-inline-block" style="width: 150px;">' + item.strDeadline + '</div></td>';
                        html += '<td><div class="h-0 d-inline-block" style="width: 150px;">' + item.strDeadline + '</div></td>';
                        html += '<td>' + item.strBody + '</td>';
                        html += '<td>' + item.strstatus + '</td>';
                        
                        html += '</tr>';
                    });

                    $('.tbody').html(html);
                    updatePaginationControls(currentPage, result.length);
                },
                error: function (errormessage) {
                    alert(errormessage.responseText);
                }
            });
        }

            function updatePaginationControls(currentPage, totalItems) {
                var totalPages = calculateTotalPages(totalItems);
                $('#paginationPrevious').prop('disabled', currentPage === 1);
                $('#paginationNext').prop('disabled', currentPage === totalPages);
            }

            $('#paginationPrevious').on('click', function () {
                if (currentPage > 1) {
                    currentPage--;
                    loadData(currentStatus, currentPage);
                }
            });

            $('#paginationNext').on('click', function () {
                var totalPages = calculateTotalPages();
                if (currentPage < totalPages) {
                    currentPage++;
                    loadData(currentStatus, currentPage);
                }
            });

            function calculateTotalPages(totalItems) {
                return Math.ceil(totalItems / pageSize);
            }
    </script>

</body>
</html>






